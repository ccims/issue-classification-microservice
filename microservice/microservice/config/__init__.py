"""The microservice configuration module.

The modules consists of configuration for the Celery application as well as
the classifiers, including label classes and paths to the pickelised trained
classifiers.
"""
